# fold-skill-into-mindmap 用户验证

## 验证说明

- 验证对象：clear-mind 技能退役后，最佳实践折叠进 `mind_map` 的 description（何时清）与返回（怎么清），主动提醒文案不再引用技能。
- 环境/前置条件：**需重启 dsh 使新构建生效**（本插件在 dsh 进程内加载，改码后旧进程仍跑旧构建）。重启后在本会话或新会话验证。

## 验证项

| 验证步骤 | 预期结果 | 实际结果 | 状态 | 备注/证据 |
| --- | --- | --- | --- | --- |
| 重启 dsh 后查看可用技能目录 | `clear-mind` 技能不再列出（catalog 中消失） | | 待验证 | |
| 在有工具调用历史的会话里触发模型调 `mind_map` | 工具描述含「该梳理的信号」①–⑥ 与「不宜」场景；返回末尾含「clear-mind playbook」段（选区间 / notes 模板 / 提交前自检 / 单独调用规则） | | 待验证 | |
| 触发主动提醒（长上下文或单轮多步）后看 system-reminder 文案 | 不再出现「clear-mind 技能」字样，改为「mind_map 返回内附 clear-mind 操作指引」 | | 待验证 | |

## 验证结论

待验证。

## 待跟进

无。
